const express = require("express")
const app = express();   //creates an instance of the Express application.
const {createServer} = require("node:http");   //importing http from node module 
//extracting createServer method from the "http" module and assigning it to a variable with the same name (createServer).
const server = createServer(app);
//The createServer method is called with app as its argument. This means that the Express application (app) will handle incoming HTTP requests.


 const { join } = require("node:path");  //the join method from the built-in "path" module in Node.js. 
//The join method is a function in the "path" module that is used to join together multiple path segments into a single path.
const path = join('/path', 'to', 'file.txt');
console.log(path);
//In this example, the join method is used to concatenate the path segments "/path", "to", and "file.txt" into a single path, which will vary based on the operating system's file path conventions. The result will be printed to the console.



// const { Server } = require("socket.io");

//The Server class is a key component of the "socket.io" library. It represents the WebSocket server that manages communication between clients and the server in real-time.
    


const io = new Server(server);